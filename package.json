{
  "name": "gets",
  "version": "1.0.0",
  "description": "Omni-channel communication app that has chat, sms, voice calls, and email capabilities",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server-dev": "cd server && npm run devStart",
    "start-client": "cd client && npm run dev",
    "start-client-network": "cd client && npm run dev-network"
  },
  "author": "Caloy",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "vite": "^6.0.11"
  }
}
